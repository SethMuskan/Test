{
  "extends": ["config:base"],
  "regexManagers": [
    {
      "fileMatch": ["template/.+\\.yaml$"],
      "matchStrings": [
        "apiVersion: .*"
      ],
      "datasourceTemplate": "kubernetes-api",
      "depNameTemplate": "{{match}}"
    }
  ],
  "labels": [
    "renovate"
  ]
}
