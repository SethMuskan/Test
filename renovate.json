{
  "extends": ["config:base"],
  "regexManagers": [
    {
      "fileMatch": ["**/*.yaml"],
      "matchStrings": [
        "apiVersion: .*"
      ],
      "datasourceTemplate": "kubernetes-api",
      "depNameTemplate": "{{match}}"
    }
  ],
  "labels": [
    "renovate"
  ]
}
