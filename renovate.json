{
  "extends": ["config:base"],
  "packageRules": [
    {
      "matchPackagePatterns": ["helm:*"],
      "matchDepTypes": ["helm"],
      "groupName": "helm-charts",
      "labels": [
        "dependencies",
        "helm"
      ]
    }
  ]
}
